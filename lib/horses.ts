export async function getLatestHorses(limit: number) {
    // Implement your database query here to fetch the latest horses
    // Return type should match the Horse interface
    // Example:
    // return prisma.horse.findMany({
    //   take: limit,
    //   orderBy: { registeredAt: 'desc' }
    // })
} 